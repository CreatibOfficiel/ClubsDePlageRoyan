<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

        {#        Menu de navigation:   #}
        <div class="w-full h-20 block flex-grow lg:flex lg:items-center lg:w-auto bg-gray-800 p-6 ">
            <div class="text-lg lg:flex-grow">
                <a href="{{ path('app_home') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Acceuil
                </a>
                <a href="{{ path('app_booking_lesson') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Réserver une leçon
                </a>
                <a href="{{ path('app_childrens_activity') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Activités enfants
                </a>
                <a href="{{ path('app_prices') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Tarifs
                </a>
                <a href="{{ path('app_contact') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Contact
                </a>
            </div>
            <div>
                <a href="{{ path('app_account_details') }}" class="block mt-4 lg:inline-block lg:mt-0 text-slate-50 hover:text-amber-300 mr-4">
                    Account Details
                </a>
                <a href="{{ path('app_login') }}" class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-gray-800 hover:bg-white mt-4 lg:mt-0">Login</a>
                <a href="{{ path('app_register') }}" class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-gray-800 hover:bg-white mt-4 lg:mt-0">Register</a>
            </div>
        </div>

        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
